require 'spec_helper'

describe JsonapiRails do
  it 'has a version number' do
    expect(JsonapiRails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
